import { FontSizeUpDirective } from './font-size-up.directive';

describe('FontSizeUpDirective', () => {
  it('should create an instance', () => {
    const directive = new FontSizeUpDirective();
    expect(directive).toBeTruthy();
  });
});
